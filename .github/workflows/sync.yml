name: Upstream Sync - OrionTV

permissions:
  contents: write

on:
  schedule:
    - cron: "0 4 * * *" # 定时触发器：每天 UTC 时间 4:00 (北京时间中午 12:00) 运行一次
  workflow_dispatch: # 允许手动从 GitHub Actions 页面触发此工作流

jobs:
  sync_latest_from_upstream:
    name: Sync latest commits from OrionTV/OrionTV
    runs-on: ubuntu-latest
    if: ${{ github.event.repository.fork }} # 确保只在 Fork 仓库中运行

    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4

      - name: Sync upstream changes
        id: sync
        uses: aormsby/Fork-Sync-With-Upstream-action@v3.4.1
        with:
          upstream_sync_repo: OrionTV/OrionTV # 请再次确认上游仓库的名称和默认分支
          upstream_sync_branch: main # 假设上游是 main，如果不是也需要修改
          target_sync_branch: master # *** 已修改为 master，因为你的 Fork 仓库默认分支是 master ***
          target_repo_token: ${{ secrets.GITHUB_TOKEN }} # 使用 GitHub 提供的默认 Token

      - name: Sync check
        if: failure()
        run: |
          echo "[Error] Due to a change in the workflow file of the upstream repository, GitHub has automatically suspended the scheduled automatic update. You need to manually sync your fork."
          exit 1
